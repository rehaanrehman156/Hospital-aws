name: Release and deploy dev

on:
  push:
    branches:
      - main
  workflow_dispatch:

concurrency:
  group: release-main
  cancel-in-progress: true

permissions:
  contents: write

jobs:
  build-and-push-backend:
    name: Build backend image and push to ECR
    runs-on: ubuntu-latest
    if: github.event_name == 'workflow_dispatch' || !contains(github.event.head_commit.message, '[skip ci]')
    outputs:
      image_uri: ${{ steps.image.outputs.image_uri }}
      image_tag: ${{ steps.image.outputs.image_tag }}
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Check backend deployment secrets
        id: backend_check
        run: |
          if [ -n "${{ secrets.AWS_ACCESS_KEY_ID }}" ] && [ -n "${{ secrets.AWS_SECRET_ACCESS_KEY }}" ] && [ -n "${{ secrets.AWS_REGION }}" ] && [ -n "${{ secrets.ECR_REPOSITORY }}" ]; then
            echo "ready=true" >> "$GITHUB_OUTPUT"
          else
            echo "ready=false" >> "$GITHUB_OUTPUT"
          fi

      - name: Fail when backend secrets are missing
        if: steps.backend_check.outputs.ready != 'true'
        run: |
          echo "Missing one or more required backend deployment secrets."
          exit 1

      - name: Configure AWS credentials
        uses: aws-actions/configure-aws-credentials@v4
        with:
          aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws-region: ${{ secrets.AWS_REGION }}

      - name: Login to Amazon ECR
        uses: aws-actions/amazon-ecr-login@v2

      - name: Build and push backend image
        id: image
        env:
          AWS_REGION: ${{ secrets.AWS_REGION }}
          ECR_REPOSITORY: ${{ secrets.ECR_REPOSITORY }}
        run: |
          set -e
          ACCOUNT_ID=$(aws sts get-caller-identity --query Account --output text)
          IMAGE_URI="${ACCOUNT_ID}.dkr.ecr.${AWS_REGION}.amazonaws.com/${ECR_REPOSITORY}"
          IMAGE_TAG="${GITHUB_SHA::7}"

          aws ecr describe-repositories --region "$AWS_REGION" --repository-names "$ECR_REPOSITORY" >/dev/null 2>&1 || \
            aws ecr create-repository --region "$AWS_REGION" --repository-name "$ECR_REPOSITORY"

          docker build -t "${IMAGE_URI}:${IMAGE_TAG}" apps/backend
          docker tag "${IMAGE_URI}:${IMAGE_TAG}" "${IMAGE_URI}:latest"
          docker push "${IMAGE_URI}:${IMAGE_TAG}"
          docker push "${IMAGE_URI}:latest"

          echo "image_uri=${IMAGE_URI}" >> "$GITHUB_OUTPUT"
          echo "image_tag=${IMAGE_TAG}" >> "$GITHUB_OUTPUT"

  update-dev-manifest:
    name: Update dev manifest for Argo CD
    runs-on: ubuntu-latest
    needs: build-and-push-backend
    if: needs.build-and-push-backend.result == 'success'
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Update dev image tag
        env:
          IMAGE_URI: ${{ needs.build-and-push-backend.outputs.image_uri }}
          IMAGE_TAG: ${{ needs.build-and-push-backend.outputs.image_tag }}
        run: |
          set -e
          sed -i -E "s|^(\s*image:\s*).*$|\1${IMAGE_URI}:${IMAGE_TAG}|g" kubernetes/dev/backend-deployment.yaml

      - name: Commit dev manifest update
        run: |
          set -e
          if git diff --quiet -- kubernetes/dev/backend-deployment.yaml; then
            echo "No dev manifest changes to commit."
            exit 0
          fi

          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add kubernetes/dev/backend-deployment.yaml
          git commit -m "ci: update dev backend image to ${GITHUB_SHA::7} [skip ci]"
          git push

  deploy-frontend:
    name: Deploy frontend to S3
    runs-on: ubuntu-latest
    if: github.event_name == 'workflow_dispatch' || !contains(github.event.head_commit.message, '[skip ci]')
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 18
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Build frontend app
        env:
          VITE_API_BASE_URL: ${{ secrets.VITE_API_BASE_URL }}
        run: npm run build --workspace @hospital/frontend

      - name: Check frontend deploy secrets
        id: frontend_check
        run: |
          if [ -n "${{ secrets.AWS_ACCESS_KEY_ID }}" ] && [ -n "${{ secrets.AWS_SECRET_ACCESS_KEY }}" ] && [ -n "${{ secrets.AWS_REGION }}" ] && [ -n "${{ secrets.S3_BUCKET_NAME }}" ]; then
            echo "ready=true" >> "$GITHUB_OUTPUT"
          else
            echo "ready=false" >> "$GITHUB_OUTPUT"
          fi

          if [ -n "${{ secrets.CLOUDFRONT_DISTRIBUTION_ID }}" ]; then
            echo "cloudfront_ready=true" >> "$GITHUB_OUTPUT"
          else
            echo "cloudfront_ready=false" >> "$GITHUB_OUTPUT"
          fi

      - name: Skip frontend deploy (secrets missing)
        if: steps.frontend_check.outputs.ready != 'true'
        run: echo "Frontend deploy skipped. Missing one or more required secrets."

      - name: Configure AWS credentials
        if: steps.frontend_check.outputs.ready == 'true'
        uses: aws-actions/configure-aws-credentials@v4
        with:
          aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws-region: ${{ secrets.AWS_REGION }}

      - name: Upload HTML with short cache
        if: steps.frontend_check.outputs.ready == 'true'
        run: |
          aws s3 sync ./apps/frontend/dist s3://${{ secrets.S3_BUCKET_NAME }} --delete \
            --exclude "*" --include "*.html" \
            --cache-control "public, max-age=3600, must-revalidate"

      - name: Upload assets with long cache
        if: steps.frontend_check.outputs.ready == 'true'
        run: |
          aws s3 sync ./apps/frontend/dist s3://${{ secrets.S3_BUCKET_NAME }} --delete \
            --exclude "*.html" \
            --cache-control "public, max-age=31536000, immutable"

      - name: Invalidate CloudFront cache (optional)
        if: steps.frontend_check.outputs.ready == 'true' && steps.frontend_check.outputs.cloudfront_ready == 'true'
        run: |
          aws cloudfront create-invalidation \
            --distribution-id "${{ secrets.CLOUDFRONT_DISTRIBUTION_ID }}" \
            --paths "/*"
